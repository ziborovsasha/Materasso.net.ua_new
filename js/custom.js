$(function(){	// nav open		$('.btn-nav').click(function(){		$('.nav-list').toggleClass('active')		// $('.nav-list').removeClass('active')		return false;	});	$('.slider-sale').slick({		slidesToScroll: 1,		// dots: true,		autoplay: true,		autoplaySpeed: 2500,		prevArrow: '<i class="fa fa-chevron-left" aria-hidden="true"></i>',		nextArrow: '<i class="fa fa-chevron-right" aria-hidden="true"></i>'	});	$('.slider-questions-answers').slick({		slidesToScroll: 1,		infinite: true,		slidesToShow: 3,		slidesToScroll: 1,		// autoplay: true,		// autoplaySpeed: 2500,		prevArrow: '<i class="fa fa-chevron-left" aria-hidden="true"></i>',		nextArrow: '<i class="fa fa-chevron-right" aria-hidden="true"></i>'	});	// CARD-SLIDER //		$('.card-slider').slick({		slidesToShow: 1,		slidesToScroll: 1,		arrows: false,		fade: true,		asNavFor: '.card-slider-nav'	});	$('.card-slider-nav').slick({		slidesToShow: 2,		slidesToScroll: 1,		asNavFor: '.card-slider',				centerMode: true,		centerPadding: "0px",		vertical: false,		focusOnSelect: true	});	$(document).ready(function() {	$(".card-select").select2();});	// TABS CARD //	// $('.card-tab a').click(function(){	// 	$(this).parents('.card-tab').find('.card-tab-content').addClass('hide');	// 	$(this).parent().siblings().removeClass('active');	// 	var id = $(this).attr('href');	// 	$(id).removeClass('hide');	// 	$(this).parent().addClass('active');	// 	return false	//  });	$(".tab_item").not(":first").hide();	$(".card-tabs .tab").click(function() {		$(".card-tabs .tab").removeClass("active").eq($(this).index()).addClass("active");		$(".tab_item").hide().eq($(this).index()).fadeIn()	}).eq(0).addClass("active");});// jQuery(document).ready(function($){// 	var tabs = $('.card-tab');	// 	tabs.each(function(){// 		var tab = $(this),// 			tabItems = tab.find('ul.card-tab-navigation'),// 			tabContentWrapper = tab.children('ul.card-tab-content'),// 			tabNavigation = tab.find('nav');// 		tabItems.on('click', 'a', function(event){// 			event.preventDefault();// 			var selectedItem = $(this);// 			if( !selectedItem.hasClass('selected') ) {// 				var selectedTab = selectedItem.data('content'),// 					selectedContent = tabContentWrapper.find('li[data-content="'+selectedTab+'"]'),// 					slectedContentHeight = selectedContent.innerHeight();				// 				tabItems.find('a.selected').removeClass('selected');// 				selectedItem.addClass('selected');// 				selectedContent.addClass('selected').siblings('li').removeClass('selected');// 				//animate tabContentWrapper height when content changes // 				tabContentWrapper.animate({// 					'height': slectedContentHeight// 				}, 200);// 			}// 		});// 		//hide the .card-tab::after element when tabbed navigation has scrolled to the end (mobile version)// 		checkScrolling(tabNavigation);// 		tabNavigation.on('scroll', function(){ // 			checkScrolling($(this));// 		});// 	});	// 	$(window).on('resize', function(){// 		tabs.each(function(){// 			var tab = $(this);// 			checkScrolling(tab.find('nav'));// 			tab.find('.card-tab-content').css('height', 'auto');// 		});// 	});// 	function checkScrolling(tabs){// 		var totalTabWidth = parseInt(tabs.children('.card-tab-navigation').width()),// 		 	tabsViewport = parseInt(tabs.width());// 		if( tabs.scrollLeft() >= totalTabWidth - tabsViewport) {// 			tabs.parent('.card-tab').addClass('is-ended');// 		} else {// 			tabs.parent('.card-tab').removeClass('is-ended');// 		}// 	}// });